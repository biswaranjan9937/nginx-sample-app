apiVersion: v1
kind: Ingress
metadata:
  name: nginx-ingress
  namespace: sample-app
rules:
- host: nginx.example.com
  http:
    paths:
    - path: /
      pathType: Prefix
      backend:
        service:
          name: nginx-service
          port:
            number: 80